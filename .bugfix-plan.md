# Momentum Bug Fix Plan

Audit produced the following enumeration. Each item gets fixed in order, with the verification step noted.

## CRITICAL

### C1 — Pomodoro phase-transition effect re-fires and saves a phantom session
**Files:** `src/components/widgets/PomodoroTimer.tsx`
**Symptoms:** Phase-change handler saves a focus session even when nothing was actually studied (or any other phantom on every transition).
**Fix:**
- Track previous phase + previous `pomStartedAt` in refs.
- The tick handler that decides a phase has elapsed reads `pomStartedAt` once and acts only on real elapsed time.
- Re-entrancy guard: if the previous render's phase is still 'focus' and the elapsed exceeds `focusMinutes`, transition ONCE; clear/reset only on a new explicit user action.
- Cleanest: derive tick from wall clock `(Date.now() - pomStartedAt)/1000`, plus a `transitionedRef` that flips when a transition fires so the next tick in the same render commit doesn't re-fire.
- Also clear `pomIntervalRef.current` on every phase change so a stale interval can't race.

**Verify:** Manual scenario in code: if `pomPhase==='focus'`, `startedAt=t0`, `focusMinutes=25`, wall clock now `=t0+25min`, the handler fires ONCE, saves the focus session, sets phase to 'break', and the next interval tick does NOT save another session.

### C2 — QuickTimer seconds inflate academic total
**Files:** `src/lib/timer-utils.ts`
**Symptoms:** QuickTimer running on a non-academic subject still adds minutes to the PomodoroTimer academic total.
**STATUS: FIXED** (2026-08-13) — Dashboard.tsx:663 now calls `getLiveTimerSeconds(data.subjects, data.categories)` so non-academic QuickTimer seconds are excluded from the academic "Today" total.
**Fix:**
- Change `getLiveTimerSeconds` to return `{ seconds, subjectId }`.
- Have `getTotalTodayMinutes` resolve QuickTimer's subject scope via `getSessionScope` and only include the live seconds when both timer sources are academic.
- Update all callers (`Dashboard.tsx`, `PomodoroTimer.tsx`).

**Verify:** A QuickTimer running for a subject whose category is `nonAcademic` does NOT change the dashboard's "Today" total.

## HIGH

### H1 — `updateRoutineLogsForSession` mis-tags arbitrary sessions with `routineId`
**Files:** `src/lib/routine-tracker.ts`
**Fix:** Only set `routineId` when `session.source === 'autoRoutine'`. If a non-autoRoutine session already has a `routineId`, leave it (don't overwrite). Read the existing record's `routineId` first; if it's null, write only when source is autoRoutine.

### H2 — Activity untick lookup uses `createdAt` instead of the persisted `sessionId`
**Files:** `Dashboard.tsx`, `TodayChecklist.tsx`
**Fix:**
- When `markDone`/`handleYes` creates the session, store the session id on the `ActivityLog` (`sessionId?: string`).
- Untick reads `removedLog.sessionId` instead of recomputing from `createdAt`.
**STATUS: FIXED** (2026-08-13) — SchedulePage.tsx `attendActivity` now persists `log.sessionId = session.id`, and the activity `onUndo` handler deletes the session via `log.sessionId` instead of the brittle `note: 'Activity: ...'` string match (which matched nothing). Dashboard.tsx + TodayChecklist.tsx were already fixed.
- Add migration: on load, if an ActivityLog has `sessionId` missing but a session exists matching the old derivation, fill it in.

### H3 — QuickTimer falls back to `data.subjects[0]`
**Files:** `src/components/widgets/QuickTimer.tsx`
**Fix:** When `stop` is hit and the configured `subjectId` no longer points to an active subject, surface an alert and do NOT save a session. Optionally show "Subject was deleted — session discarded" undo-toast with an option to reassign.

### H4 — Category delete doesn't cascade
**Files:** `src/features/categories/CategoriesPage.tsx`
**Fix:**
- Cascade also to: projects (where `subjectId in deletedSubjectIds`), sessions (where `subjectId in deletedSubjectIds` OR `projectId in deletedProjectIds`), assignments (same), routineLogs (where the linked sessionId was deleted? — keep, the routine subject still matches), activityLogs (same).
- Each row gets `deletedAt: now, updatedAt: now`.
- Wrap in a single Dexie transaction for atomicity.
- Update undo to restore everything.

### H5 — Project totals include soft-deleted sessions
**STATUS: FIXED** (2026-08-15) — `totalMinutes` (line 234) already filtered `!s.deletedAt`. Added `!s.deletedAt` to daily effectiveMinutes (line 242) and weekly effectiveMinutes (line 244), which were still including soft-deleted sessions toward goal progress.
**Files:** `src/features/projects/ProjectsPage.tsx`
**Symptoms:** Project totals include soft-deleted sessions.
**Fix:** Add `&& !s.deletedAt` to both `totalMinutes` and `effectiveMinutes` filters (lines 234, 242-244).

### H6 — Subject delete doesn't recompute streak days
**Files:** `src/features/subjects/SubjectsPage.tsx`, possibly `src/lib/routine-tracker.ts`
**Fix:**
- Add an exported helper `recomputeStreakDayForDate(dateKey)` that recomputes a streak day's `totalMinutes` from active sessions.
- Call for every date that had sessions cascading-soft-deleted.

## MEDIUM

### M1 — FAB Add actions dispatch events that race page mount
**Files:** `Dashboard.tsx` (FAB) and page-level `useEffect` listeners (`MarksPage`, `CalendarPage`, `SubjectsPage`)
**STATUS: FIXED** (2026-08-13) — FAB now uses `navigate(route, { state: { openAdd: true } })`; MarksPage, CalendarPage, SubjectsPage read `location.state?.openAdd` in a mount effect and clear it via `navigate(pathname, { replace: true, state: null })`.
**Fix:** Replace `setTimeout(dispatchEvent, 0)` with `navigate(route, { state: { openAdd: true } })`. Each page reads the flag in a mount effect and clears it.

### M2 — `changeSubject` pomodoro path over-reports
**Files:** `src/components/widgets/PomodoroTimer.tsx`
**Fix:** Cap at `cfg.focusMinutes * 60_000`.

### M3 — `saveSessionWithMidnightCheck` allows empty subjectId
**Files:** `src/components/widgets/PomodoroTimer.tsx`
**Fix:** Early return when `actualSubjectId` is empty.

### M4 — `splitSessionAtMidnight` floor mismatch
**Files:** `src/lib/timer-persistence.ts`
**Fix:** Use actual ms for both seconds and `durationMinutes`. Reuse existing `durationMinutes = Math.max(1, Math.round(...))` only for the ID; durationSeconds reflects the actual split.

### M5 — `changeSubject` simple-mode stale `simpleSeconds`
**Files:** `src/components/widgets/PomodoroTimer.tsx`
**Fix:** Compute elapsed from `(Date.now() - simpleStartedAt) / 1000 + simplePausedOffset / 1000`.

### M6 — `deleteSelectedSessions` non-awaited cloud deletes
**Files:** `Dashboard.tsx`
**Fix:** `await Promise.all(targets.map(s => syncSessionDelete(s.id)))`.

### M7 — Auto-log widget shows ALL soft-deleted autoRoutine sessions
**Files:** `Dashboard.tsx`
**STATUS: FIXED** (2026-08-13) — Auto-log widget window narrowed from 7 days to 24h (`< 24 * 60 * 60 * 1000`). Note: `pendingConfirmation` field does not exist on Session; the codebase convention is `source === 'autoRoutine' && deletedAt` as the pending flag (documented in SPECIFICATIONS.md §4.3).
**Fix:** Filter to `pendingConfirmation === true` (set at creation) AND within 24h. Add a "Dismiss all" button.

### M8 — `flushPendingDirtyTables()` called during render
**Files:** `src/app/providers.tsx`
**Fix:** Move into a `useEffect` or prefix with `void`.

### M9 — `loadPrefs` wipes prefs on version bump
**Files:** `src/components/layout/AppLayout.tsx`
**Fix:** Don't increment `CURRENT_PREFS_VERSION` casually. Add a guard so if the saved version differs but no schema-affecting change, treat it as a passthrough.

### M10 — `RoutinePage.saveLogMinutes` doesn't create a Session
**Files:** `src/features/routines/RoutinePage.tsx`
**Fix:** Create a corresponding Session like TodayChecklist does: `startAt = now - mins*60_000`, `endAt = now`, `source = 'manual'`, `subjectId = routine.subjectId`, `projectId = routine.projectId`.

### M11 — `buildTodaySubjectBreakdown` missing soft-delete filter on subjects
**Files:** `src/lib/save-study-session.ts`
**Fix:** Add `&& !subj.deletedAt` filter.

### M12 — `use-undo` silently discards pushes
**Files:** `src/lib/use-undo.tsx`
**Fix:** Show a brief toast like "Action in progress — try again" or queue the push.

## LOW

- **L1** Delete duplicate SettingsPage constants; import from `settings-store`.
- **L2** Centralize `STREAK_MILESTONES` to `utils.ts`; import everywhere.
- **L3** Fix longest-streak consistency with current-streak rules. **STATUS: FIXED** (2026-08-13) — `use-streak.ts` longestStreak now treats any adjacent gap ≤ 2 as part of the chain (matching the current-streak rule where the gap budget replenishes on each logged day); a gap of 3+ breaks it. Added regression test in `streak.test.ts`.
- **L4** Subject picker hides children whose parent is deleted (or filter them out cleanly). **STATUS: FIXED** (2026-08-15) — `getSubjectPickerOptions` in `utils.ts` now promotes a child whose parent is soft-deleted to a top-level option, so it is no longer silently dropped from pickers (QuickTimer, Activities, Marks, Projects).
- **L5** Task select in log modal: also render when `logSubjectId` is set, regardless of project. **STATUS: STALE-SPEC** — contradicts Regression Checklist §6 (project dropdown MUST be hidden when subject has zero projects). Current behavior (hide) is correct; this entry is invalid.
- **L6** Modal drag-to-dismiss: rely on parent's `onMouseDown` only; remove inner handler. **STATUS: FIXED** (already in `Modal.tsx` before this audit) — `mouseDownTargetRef` tracks the mousedown target; `onClick` only closes if both the mousedown and the click landed on the dialog element itself. Drag-from-inside-to-outside is therefore ignored. Regression test in `modal-drag.test.ts`.
- **L7** NumberInput on empty blur: don't clamp to `min`; keep empty and run validator on submit.
- **L8** QuickTimer persist: remove `seconds` from effect deps.
- **L9** FreeformWidget resize: keep inline style until state commit lands.
- **L10** FreeformWidget cascade: don't overwrite saved x/y on first switch.
- **L11** Bulk-update routines/subjects in transactions. **STATUS: FIXED** (2026-08-13) — RoutinePage.saveGrid + undo/redo wrapped in `db.transaction('rw', db.routines, ...)`; SubjectsPage delete undo/redo wrapped in the same 4-table transaction as the delete path.
- **L12** Today widget live timer source for streak widget — feed same live minutes. **STATUS: FIXED** (2026-08-13) — streak widget now uses `liveTotalTodayMinutes` (includes live timer) instead of persisted-only `todayMinutes` for its "at risk" / "log today" decisions.
- **L13** Recent-session grouping TZ verify — already OK with `toLocalDateString`.

## Verification gates per item

For each item applied I will:
1. Re-read the patched section via `read`.
2. Run `npx tsc --noEmit` (whole-flow).
3. If the item is a behaviour change I can probe via a unit test (`vitest` exists in `momentum/`), add a regression test.

End-state verification:
1. `npx tsc --noEmit` — must pass.
2. Optional: `npx vitest run` if a few targeted unit tests can be written cheaply.
3. Manual reasoning sketch of each CRITICAL/HIGH scenario passes.
